import { describe, it, expect, vi } from 'vitest';
import { uploadFileResumable } from './upload';

describe('uploadFileResumable', () => {
  it('should be defined', () => {
    console.log(vi); // Dummy use
    expect(uploadFileResumable).toBeDefined();
  });
});
