# ── AG Associates — Production Environment Template ──────────────────
# This file is copied to /srv/ag/.env by bootstrap-vps.sh.
# Replace every REPLACE_WITH_* value before first boot.
#
# Generate secrets:  openssl rand -hex 32
# Hash n8n password:  docker run --rm caddy:2 caddy hash-password --plaintext 'your-password'

# ── Domain ───────────────────────────────────────────────────────────
DOMAIN=advaiityagade.com
ACME_EMAIL=admin@advaiityagade.com

# ── PostgreSQL ───────────────────────────────────────────────────────
POSTGRES_USER=agadmin
POSTGRES_PASSWORD=REPLACE_WITH_secure_password
POSTGRES_DB=agdb

# ── Redis ────────────────────────────────────────────────────────────
REDIS_PASSWORD=REPLACE_WITH_secure_redis_password

# ── LLM / AI ─────────────────────────────────────────────────────────
LLM_BASE_URL=https://api.groq.com/openai/v1
LLM_MODEL_NAME=llama-3.3-70b-versatile
LLM_API_KEY=REPLACE_WITH_groq_api_key
LLM_MOCK_MODE=false

# ── Supabase ─────────────────────────────────────────────────────────
SUPABASE_URL=REPLACE_WITH_supabase_project_url
SUPABASE_SERVICE_ROLE_KEY=REPLACE_WITH_supabase_service_role_key
SUPABASE_ANON_KEY=REPLACE_WITH_supabase_anon_key
SUPABASE_JWT_SECRET=REPLACE_WITH_supabase_jwt_secret

# ── Auth ─────────────────────────────────────────────────────────────
JWT_SECRET=REPLACE_WITH_openssl_rand_hex_32

# ── n8n ──────────────────────────────────────────────────────────────
N8N_WEBHOOK_KEY=REPLACE_WITH_openssl_rand_hex_32
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD_HASH=REPLACE_WITH_caddy_hash_password

# ── Telegram Bot ─────────────────────────────────────────────────────
TELEGRAM_BOT_TOKEN=REPLACE_WITH_bot_token_from_botfather

# ── Open-source public voice ─────────────────────────────────────────
VOICE_SYSTEM_ENABLED=true
PUBLIC_VOICE_MAX_AUDIO_BYTES=8388608
# Use large-v3-turbo on a GPU host; base is the portable development default.
WHISPER_MODEL_SIZE=base
# Mount the selected Piper voice model under the AG voice-model volume.
PIPER_VOICE_PATH=/models/piper/hi_IN-pratham-medium.onnx

# ── Email Intake ─────────────────────────────────────────────────────
EMAIL_IMAP_HOST=REPLACE_WITH_imap_host
EMAIL_IMAP_PORT=993
EMAIL_IMAP_USER=REPLACE_WITH_imap_user
EMAIL_IMAP_PASS=REPLACE_WITH_imap_password
EMAIL_POLL_INTERVAL=60

# ── Google Gemini (optional) ─────────────────────────────────────────
GOOGLE_GENERATIVE_AI_API_KEY=

# ── Resend (optional) ────────────────────────────────────────────────
RESEND_API_KEY=

# ── NeSL e-Filing ────────────────────────────────────────────────────
NESL_API_KEY=
NESL_API_BASE_URL=https://api.nesl.co.in/v1
NESL_CLIENT_ID=
NESL_CLIENT_SECRET=
NESL_MOCK_DELAY_SEC=3

# ── IGR Portal ───────────────────────────────────────────────────────
IGR_PORTAL_URL=https://igrmaharashtra.gov.in/efiling/
IGR_PORTAL_USERNAME=
IGR_PORTAL_PASSWORD=

# ── GRAS Portal ──────────────────────────────────────────────────────
GRAS_PORTAL_URL=https://gras.mahakosh.gov.in/echallan/

# ── Runtime ──────────────────────────────────────────────────────────
ENVIRONMENT=production
IMAGE_TAG=latest

# ── Encrypted backups ────────────────────────────────────────────────
# Keep the repository and password outside Git; use an off-site Restic
# repository (for example Backblaze B2 or an S3-compatible provider).
RESTIC_REPOSITORY=
BACKUP_ENCRYPTION_KEY=REPLACE_WITH_restic_password
# Compose project-prefixed volume names used by the production stack.
BACKUP_VOLUMES=ag_ag_output ag_ag_documents ag_n8n_data ag_caddy_data ag_caddy_config
