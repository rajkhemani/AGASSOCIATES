interface:
  display_name: "AGASSOCIATES"
  short_description: "Repo-specific patterns and workflows for AGASSOCIATES"
  default_prompt: "Use the AGASSOCIATES repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true