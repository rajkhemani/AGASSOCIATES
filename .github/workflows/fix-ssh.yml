name: Fix SSH and Deploy
on: workflow_dispatch
jobs:
  fix:
    runs-on: self-hosted
    steps:
    - run: |
        set -ex
        id deploy || useradd -m deploy
        mkdir -p ~deploy/.ssh /root/.ssh
        echo "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAILa+pYqBIW5s4nYrmZrESiJmtJfodfI+aeSP8BcYo6Xj rajkhemani24@gmail.com" > ~deploy/.ssh/authorized_keys
        echo "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIFBdZMtB65G0IIVz3MGIA4LeelHHo0zSOq7P5BdnoW6I luxor9@luxor9-Victus-by-HP-Laptop-16-d0xxx" >> ~deploy/.ssh/authorized_keys
        cp ~deploy/.ssh/authorized_keys /root/.ssh/authorized_keys
        chmod 600 /root/.ssh/authorized_keys ~deploy/.ssh/authorized_keys
        chown -R deploy:deploy ~deploy/.ssh
        usermod -aG docker deploy
        systemctl enable ssh 2>/dev/null || true
        systemctl start ssh 2>/dev/null || true

    - run: |
        set -ex
        if [ ! -f /srv/ag/.env ]; then
          mkdir -p /srv/ag
          PW1=$(openssl rand -hex 16)
          PW2=$(openssl rand -hex 16)
          JWT=$(openssl rand -hex 64)
          NWK=$(openssl rand -hex 32)
          echo "DOMAIN=advadiityagade.com" > /srv/ag/.env
          echo "ACME_EMAIL=rajkhemani24@gmail.com" >> /srv/ag/.env
          echo "POSTGRES_USER=agadmin" >> /srv/ag/.env
          echo "POSTGRES_PASSWORD=$PW1" >> /srv/ag/.env
          echo "POSTGRES_DB=agdb" >> /srv/ag/.env
          echo "REDIS_PASSWORD=$PW2" >> /srv/ag/.env
          echo "LLM_BASE_URL=https://api.groq.com/openai/v1" >> /srv/ag/.env
          echo "LLM_MODEL_NAME=llama-3.3-70b-versatile" >> /srv/ag/.env
          echo "LLM_MOCK_MODE=true" >> /srv/ag/.env
          echo "SUPABASE_URL=https://kglqdihggncxkwwsgguw.supabase.co" >> /srv/ag/.env
          echo "SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImtnbHFkaWhnZ25jeGt3d3NnZ3V3Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3Nzc0MDEzMDUsImV4cCI6MjA5Mjk3NzMwNX0.fFnHP2GAZcXEDpeI58uxYYwO5UliQFKG65I5plX01MA" >> /srv/ag/.env
          echo "JWT_SECRET=$JWT" >> /srv/ag/.env
          echo "N8N_WEBHOOK_KEY=$NWK" >> /srv/ag/.env
          echo "ENVIRONMENT=production" >> /srv/ag/.env
          echo "IMAGE_TAG=latest" >> /srv/ag/.env
          chmod 600 /srv/ag/.env
          echo "Created /srv/ag/.env"
        fi

    - run: |
        set -ex
        cd /home/luxor9/actions-runner/_work/AGASSOCIATES/AGASSOCIATES
        docker compose -f docker-compose.prod.yml --env-file /srv/ag/.env build
        docker compose -f docker-compose.prod.yml --env-file /srv/ag/.env up -d --remove-orphans
        docker image prune -f
        docker compose -f docker-compose.prod.yml ps
