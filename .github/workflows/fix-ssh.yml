name: Fix SSH and Deploy
on: workflow_dispatch
jobs:
  fix:
    runs-on: self-hosted
    steps:
    - run: |
        set -ex
        id deploy || useradd -m deploy
        mkdir -p ~deploy/.ssh /root/.ssh
        cat > ~deploy/.ssh/authorized_keys << 'KEYS'
        ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAILa+pYqBIW5s4nYrmZrESiJmtJfodfI+aeSP8BcYo6Xj rajkhemani24@gmail.com
        ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIFBdZMtB65G0IIVz3MGIA4LeelHHo0zSOq7P5BdnoW6I luxor9@luxor9-Victus-by-HP-Laptop-16-d0xxx
        KEYS
        sed -i 's/^[[:space:]]*//' ~deploy/.ssh/authorized_keys
        cp ~deploy/.ssh/authorized_keys /root/.ssh/authorized_keys
        chmod 600 /root/.ssh/authorized_keys ~deploy/.ssh/authorized_keys
        chown -R deploy:deploy ~deploy/.ssh
        usermod -aG docker deploy
        systemctl enable ssh 2>/dev/null || true
        systemctl start ssh 2>/dev/null || true

    - run: |
        set -ex
        if [ ! -f /srv/ag/.env ]; then
          mkdir -p /srv/ag
          PW1=$(openssl rand -hex 16)
          PW2=$(openssl rand -hex 16)
          JWT=$(openssl rand -hex 64)
          NWK=$(openssl rand -hex 32)
          cat > /srv/ag/.env << 'ENVEOF'
          DOMAIN=advadiityagade.com
          ACME_EMAIL=rajkhemani24@gmail.com
          POSTGRES_USER=agadmin
          POSTGRES_PASSWORD=$PW1
          POSTGRES_DB=agdb
          REDIS_PASSWORD=$PW2
          LLM_BASE_URL=https://api.groq.com/openai/v1
          LLM_MODEL_NAME=llama-3.3-70b-versatile
          LLM_MOCK_MODE=true
          SUPABASE_URL=https://kglqdihggncxkwwsgguw.supabase.co
          SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImtnbHFkaWhnZ25jeGt3d3NnZ3V3Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3Nzc0MDEzMDUsImV4cCI6MjA5Mjk3NzMwNX0.fFnHP2GAZcXEDpeI58uxYYwO5UliQFKG65I5plX01MA
          JWT_SECRET=$JWT
          N8N_WEBHOOK_KEY=$NWK
          ENVIRONMENT=production
          IMAGE_TAG=latest
ENVEOF
          sed -i 's/^[[:space:]]*//' /srv/ag/.env
          chmod 600 /srv/ag/.env
          echo "Created /srv/ag/.env"
        fi

    - run: |
        set -ex
        cd /home/luxor9/actions-runner/_work/AGASSOCIATES/AGASSOCIATES
        docker compose -f docker-compose.prod.yml --env-file /srv/ag/.env build
        docker compose -f docker-compose.prod.yml --env-file /srv/ag/.env up -d --remove-orphans
        docker image prune -f
        docker compose -f docker-compose.prod.yml ps
